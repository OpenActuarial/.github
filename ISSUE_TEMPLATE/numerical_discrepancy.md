---
name: Numerical discrepancy
about: A calculation disagrees with a reference you trust
labels: numerics
---

**What was calculated** (package, function, version — `pip show <package>`):

**Minimal reproducible input** (a few rows of data + the exact call):

```python
```

**Result obtained:**

**Result expected, and the reference for it** (paper/textbook/formula,
another library, or a hand calculation — show it):

**Environment** (`python -V`, numpy/pandas/scipy versions):
